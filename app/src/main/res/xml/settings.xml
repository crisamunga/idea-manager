<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">


    <ListPreference
        android:key="locale"
        android:title="@string/language"
        android:summary="@string/preferred_language_s"
        android:dialogTitle="@string/language"
        android:entries="@array/languages"
        android:icon="@drawable/ic_usa_flag_circular"
        android:entryValues="@array/language_values"
        android:defaultValue="en" />

    <SwitchPreference
        android:key="biometrics"
        android:title="@string/enable_biometrics"
        android:summary="@string/use_fingerprint_to_unlock_the_app"
        android:defaultValue="false"
        app:isPreferenceVisible="false"
        android:icon="@drawable/ic_baseline_fingerprint_24"/>

</PreferenceScreen>